local config = {
    mod = "SUPER",

    apps = {
        terminal = "kitty",
        fileManager = "kitty -e yazi",
        menu = "rofi -show drun",
        browser = "firefox",
    }
}

return config
